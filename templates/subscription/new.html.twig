{% extends 'base.html.twig' %}

{% block body %}
    {{ form(form) }}

    {# alternative method to add submit-button #}

    {# {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Save</button>
    {{ form_end(form) }} #}
{% endblock %}